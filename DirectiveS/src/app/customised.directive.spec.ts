import { CustomisedDirective } from './customised.directive';

describe('CustomisedDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomisedDirective();
    expect(directive).toBeTruthy();
  });
});
